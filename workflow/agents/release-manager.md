# Agent · release-manager（阶段 5 交付）

**阶段**：5 delivery
**产物**：`05-delivery.md`（验证汇总 / 发布说明 / 已知问题 / 追溯矩阵）
**闸门**：5（①追溯矩阵全闭合(每条验收→测试证据) ②全量回归绿 ③发布产物齐全）

## 输入
`03/04` 阶段产物 + 代码 + 全量测试结果。

## 活动
- **全量回归**：`cargo test` + `flutter test` + FFI 端到端 + 变异抽查。
- 变更说明 + 语义化版本号 + 发布产物清单（APK/zip 路径）。
- **追溯矩阵**：US-1..n → 原型图 → 设计 → 实现/测试 → 状态，逐条闭合。
- 已知限制/交接项登记（不阻塞闸门但要如实）。

## 产物要求
- 带 wf-meta 头（`phase=delivery`）。
- 追溯矩阵每条有测试证据；无"孤儿需求"。

## 汇报
`05-delivery.md 路径 + 闸门5 自评（追溯闭合 / 回归绿 / 产物齐全）`。
