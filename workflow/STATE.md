# 工作流状态（唯一状态源 —— 仅 orchestrator 可写，Agent 只读）

| 字段 | 值 |
|---|---|
| 当前阶段 | **REQ-006 全部阶段完成（闸门1-5 全 ✅）**，分支 `wf/REQ-006-tts-online-translate` 待用户确认后合并 main |
| 活跃 REQ | REQ-006-tts-online-translate（P0 听书无声/不滚动 + P0 新增在线翻译） |
| 分支 | wf/REQ-006-tts-online-translate（已提交 6 个 commit：`e2e5798` 开发 / `6b91de7` 测试 / `0eb667c` 清理 / `4b3c49c` 产品验收 / `a8f909b`+`834faaf` 交付） |
| 闸门状态 | 闸门1 ✅（orchestrator 独立复验：wf-meta exit 0；US-1..US-23 全可测、每条标注 [配置断言]/[单测]/[真机]；根因 R1-1..R1-8/R2-1..R2-4 带 file:line 证据；影响面四类非空；与 REQ-003/004/005 复用不重复）。闸门2 ✅（`02-adr` 7 决策点各 3 备选+理由+影响+降级线；`02-design` 接口/时序/逐屏映射/错误文案契约/13 条授权取舍；`02-plan` T-001..T-012 ≤1d、US 覆盖全闭合、DAG 无环、8 类冲突已处置）。闸门3 ✅（orchestrator 独立复验：cargo test --release **221 passed/0 failed**；flutter test **106 passed/4 skipped**；flutter analyze **0**；ddd-lint **违规=0**；CRAP **FAIL=0/WARN=7**（新增 2 个，744 新增行 1%=7.44 内）；codegen 幂等；manifest 含 TTS_SERVICE+INTERNET；无硬编码 key；原型 deviation=0）。闸门4 ✅（orchestrator 独立复验：变异 **119/120=99.17%**（含 timeout 97.54%）≥80%；新代码覆盖 Rust **268/275=97.5%**、Dart **170/170=100%** ≥85%；存活体 1 等价豁免 + timeout 2 死循环隔离复核，**全部有结论**）。闸门5a ✅（product-reviewer：`ui-screenshots.sh REQ-006` 真实渲染 5 屏，HTML 报告 + manifest，**deviation=0**，无 rework）。闸门5 ✅（release-manager：追溯矩阵 **US-1..23 = 23/23 闭合、孤儿=0**；全量回归绿；FFI 3/3；DDD 0/CRAP FAIL=0；v0.7.0+11；**Android APK 构建成功** `dist/reader-android-arm64-v0.7.0.apk` 46MB（aapt2 校验 versionName=0.7.0/versionCode=11、含 INTERNET+TTS_SERVICE+PROCESS_TEXT、3 ABI .so）；Linux bundle 39MB/version 0.7.0+11） |
| rework 计数 | REQ-006：**0**（无 rework-A/B/C/D；orchestrator 仅做 1 处 trivial 清理 commit `0eb667c` 去除 REQ-006 引入的 unused_mut 警告） |
| 最近事件 | REQ-006 **全流程完成**。用户两项 P0：① 听书无声/不滚动 → 根因 Android 主清单缺 `TTS_SERVICE` 包可见性 + 引擎未 `setLanguage`/`awaitSpeakCompletion`/`focus`/`speak` 失败静默 + 跟读组件无滚动控制；已修复并加"当前句自动滚动进视口"（`offsetForHighlight` 纯函数 + `TextPainter` + `animateTo`）。② 在线翻译 → 新增 `provider="auto"`（在线优先 DeepL→无 key/失败回退 offline + 明确文案）、`translate_get_config`/`translate_set_strategy` 桥接 + 设置页策略下拉/key 掩码回填、译文卡片来源标签修正、主清单 `INTERNET`（release 联网必需）；**无硬编码 key**。orchestrator 独立复验全部闸门（不采信自评）：cargo 221/0、flutter 106/4skip、analyze 0、ddd 0、CRAP FAIL=0、变异 99.17%、覆盖 ≥85%、原型 deviation=0、追溯 23/23、APK 构建成功。 |
| 遗留问题（非阻塞） | ① **真机 8 项手工验收**（真正出声/中文语言/音频焦点/自动滚动/release 联网/真实 DeepL/音色可用性等，见 `03-review.md §4`）需具设备与用户 DeepL key 的环境执行；② DeepL 单请求 >128 KiB 不自动分段（明确错误/回退离线，授权取舍10）；③ 朗读语言固定 `zh-CN`（多语言留后续 REQ）；④ key 明文存 settings（加密/钥匙串留后续 REQ）；⑤ 音色枚举差异下男女声切换名存实亡（显示"系统默认音色"）；⑥ REQ-005 遗留警告 `core/src/tts/mod.rs:572 unused text`（非本 REQ）；⑦ `app/lib/widgets/**` 未被 ddd-rules 声明层（沿用 REQ-005 人工核对，建议后续评审纳入 interface）；⑧ 横屏设计稿↔竖屏实现建议补 390×844 稿。 |
| 下一条建议 | ① 用户在 Android 真机执行 `03-review.md §4` 8 项验收（APK：`dist/reader-android-arm64-v0.7.0.apk`），并在设置页填入自己的 DeepL key 验证在线翻译；② 确认后 `git checkout main && git merge wf/REQ-006-tts-online-translate`（orchestrator 不自行合并）；③ 可选：补竖屏设计稿 / 将 `app/lib/widgets` 纳入 ddd-rules interface 层 / 后续 REQ 处理 key 加密与多语言朗读。 |
