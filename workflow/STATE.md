# 工作流状态（唯一状态源 —— 仅 orchestrator 可写，Agent 只读）

| 字段 | 值 |
|---|---|
| 当前阶段 | **REQ-007 阶段5a 产品验收（进行中）** —— 分支 `wf/REQ-007-reader-interaction-fixes` |
| 活跃 REQ | REQ-007-reader-interaction-fixes（P0 阅读器交互 bug：①点击正文中部无法呼出顶底栏 ②无法切换下一章 ③在线翻译引导） |
| 分支 | wf/REQ-007-reader-interaction-fixes（基于 main `4f3f55b`） |
| 闸门状态 | 闸门1 ✅（orchestrator 独立复验：`wf-meta-check` 全部头合法；`01-req.md` 336 行、US-1..US-17 全可测且逐条标注 [集成测试]/[widget 测试]/[单测]/[真机]；根因 R1-1/R1-2/R2-1/R2-2/R2-3/R3-1/R3-2/R3-3 带 file:line 证据；无重复（复用 REQ-001/003/004/005/006）；影响面 5 个子面非空；req-analyst 新增发现 R3-3：SettingsPage 运行期不可达，故"去设置"须直接 push）。闸门2 ✅（orchestrator 独立复验：`02-adr` 6 决策点 D1..D6 各 3-4 备选+选择理由+影响+降级线；`02-design` 接口/时序/逐屏映射/DDD 分层/零 schema；`02-plan` T-001..T-009 各 ≤1d、US-1..17 覆盖矩阵全闭合、DAG 无环、8 类冲突已处置；可测出口 PagedDocumentReloader/parseJsBool+PagedJsExecutor/PagedViewControls+PageTurnCoordinator 落定）。闸门3 ✅（orchestrator 独立复验，不采信自评：cargo test --release **222 passed/0 failed**；flutter test **149 passed/4 skipped**；flutter analyze **0**；ddd-lint **违规=0**；集成测试 `reader_interaction_test` **5 passed/0 failed** + `screenshots_test` **12 passed/0 failed**（截图重产）；`no_synthetic_chrome_test` 静态守卫绿、`integration_test/` 无 `ReaderTopBar(`/`ReaderBottomBar(` 合成构造；**orchestrator 独立对照实验**：同一布局/落点，旧 `GestureDetector(onTapUp)` 包 `SelectionArea` 点文字 → `toggled=0`，新 `Listener` 手动判定 → `toggled=1`，证实 R1-1 根因与修复有效；原型 deviation=0（`reader_chrome.dart`/`selection_toolbar.dart` 零改动，唯一 UI 增量=错误浮层"去设置"）；CRAP N/A（core 仅字符串字面量追加+测试，无逻辑/分支变更，沿用 REQ-006 FAIL=0 基线）；codegen 零改动）。闸门4 ✅（orchestrator 独立复验：变异——core 仅字符串字面量追加，`cargo mutants --list` 102 个与 REQ-006 基线逐条一致（0 增 0 删），改动行 0 个变异体，定向重跑 `translate_auto` **5 caught/1 missed/0 timeout/1 unviable=83.3%** ≥80%，基线校正 **118/120=98.33%**；存活体 **4/4 有结论**（`492` 既有测试缺口+后续补测、`351` 等价豁免、`285/287` 死循环 timeout）；覆盖——本 REQ 新增/改动 Dart 代码 **142/142=100%**、whole-file 477/515=92.6%（排除固有不可测 `paged_web_view.dart` 真实 WebView）≥85%；orchestrator 独立复算 lcov：`body_tap_policy`/`paged_document_reloader`/`paged_js_result`/`paged_view_controls`/`translation_popup` 均 100%、`reader_page` 89.1%；cargo 222/0、flutter **152 passed/4 skipped**；无 rework-D）。闸门5a/5b 未开始 |
| rework 计数 | REQ-007：A=0 B=0 C=0 D=0（test-engineer 发现 `translation.rs:492` 为 REQ-006 遗留测试缺口，非本 REQ 缺陷，不触发 D） |
| 最近事件 | orchestrator 启动 REQ-007。已做**预调查**（供各阶段参考，非自评）：<br>**P1 根因已复现**：`ReaderPage` 用 `GestureDetector.onTapUp` 包 `SelectionArea`（`reader_page.dart:483-492`）。widget 探针证明——点击**正文文字**（rect.center）后 `_chromeVisible` 不翻转（`find.byTooltip('返回书架')`=0），点击**文字下方空白**则翻转（=1）。即 `SelectionArea` 的手势识别器在手势竞技场中赢得 tap，吞掉父 `GestureDetector` 的 `onTapUp`。真机正文铺满屏幕，用户几乎只能点到文字 → 顶底栏永不出。REQ-005 集成测试之所以"通过"，是 `tapAt(195,422)` 落在 `_LongFakeBackend` 文字下方空白处（非真实场景），"呼出顶底栏"截图仍用合成 `Column` 绕过（`screenshots_test.dart:147-179`）。<br>**P2 根因（两处，待架构/开发复核）**：①`PagedWebView` 只在 `didUpdateWidget` 处理 fontSize/theme（`paged_web_view.dart:70-75`），不处理 `href`/`html` 变化；`InAppWebView` 的 `initialData` 仅在创建时生效（插件 `in_app_webview.dart:307` 传入 creation params），故分页模式切章后 WebView 仍显示旧章 → 底栏"下一章"/章末续章视觉上无效。②`PagedWebViewState._runBool` 把 `evaluateJavascript` 返回值与字符串 `'true'` 比较（`paged_web_view.dart:108-113`），而插件对 JS 布尔结果做 `json.decode` 返回 Dart `bool`（插件 `in_app_webview_controller.dart:1923-1935`），故 `nextPage/prevPage` **恒返回 false** → `_page()` 恒回退 `_goChapter()`（`reader_page.dart:211-216`），分页模式边缘点击直接跳章、永不翻页。<br>**P3 现状**：`auto` 策略已在 `translate_auto`（`core/src/dict/translation.rs:451-570`）实现"在线优先→失败/无 key 回退离线"，有 key 时走 DeepL；无 key 且离线未命中时错误文案为"未配置在线翻译 API Key（deepl），且离线翻译未命中（请先安装内置词库）"，但 `ReaderPage._doTranslate` 仅把它塞进 `OverlayError`（`reader_page.dart:386-392`、`545-546`），**无指向设置页的引导入口**。需确认 auto+key 触发在线翻译，并补"去设置"引导。 |
| 约束/要求（用户） | ① 维护 STATE.md；② 逐阶段独立 subagent + 独立闸门复验（不采信自评）；③ 交互 bug 必须用真实 integration_test（真实 ReaderPage 点击/手势）验证，禁止合成页绕过；④ cargo test + flutter test 全绿；⑤ `bash scripts/ui-screenshots.sh REQ-007` 产真实截图；⑥ `bash scripts/build-android-local.sh` 产 APK `dist/reader-android-arm64-vX.Y.Z.apk`；⑦ 阻塞则写 STATE 并停。 |
| 环境事实 | flutter 3.47.2（`/root/flutter`）；cargo 1.98.1；NDK 27.0.12077973/28.2.13676358；JDK 21；`flutter_inappwebview` **不支持 Linux**（pubspec platforms 无 linux）→ 分页模式无法在 Linux integration_test 实跑，集成测试须用滚动模式真实 ReaderPage；分页相关以 widget 测试 + 纯函数单测覆盖。基线：flutter test **106 passed/4 skipped**；cargo test --release **221 passed/0 failed**（含 1 条既有 warning `unused variable: text`）。 |

## 阶段进度

| 阶段 | Agent | 产物 | 状态 |
|---|---|---|---|
| 1 需求 | req-analyst | `01-req.md` | ✅ 完成（闸门1 通过） |
| 2 架构 | architect | `02-adr.md` `02-design.md` `02-plan.md` | ✅ 完成（闸门2 通过） |
| 3 开发 | developer | 代码 + `03-review.md` | ✅ 完成（闸门3 通过，commit `d20f789`） |
| 4 测试 | test-engineer | `04-mutation.md` `04-coverage.md` | ✅ 完成（闸门4 通过，commit `28d5821`） |
| 5a 产品验收 | product-reviewer | `05b-product-preview.md` + HTML | ⏳ 进行中 |
| 5b 交付 | release-manager | `05-delivery.md` + APK | 待开始 |
