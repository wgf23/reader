<!-- wf-meta: req=REQ-XXX | phase=architecture | agent=architect | date=YYYY-MM-DD | gate=failed -->
# REQ-XXX · 模块/接口设计

## 1. 模块与职责变化
（新增/修改模块，归属哪一层：interface/application/domain/infrastructure）

## 2. 接口签名（Rust / Dart）
```rust
// 示例
```

## 3. 数据模型变化
（表/结构体/字段；DDL 或结构定义）

## 4. 关键时序
（伪代码/时序：入口 → 处理 → 落库/渲染）

## 5. 与既有约定的兼容性
- [ ] 不破坏 Locator 模型
- [ ] 不跨越限界上下文（docs/04 §1）
- [ ] 听读同进度不变式保持
